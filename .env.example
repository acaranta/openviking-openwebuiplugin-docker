# OpenViking server base URL (reachable from the container)
OV_ENDPOINT=http://openviking:1933
# Bearer token for OpenViking - never commit the real value
OV_API_KEY=
OV_ACCOUNT=default
OV_USER=default
OV_AGENT=default
OV_TIMEOUT=30
